<?xml version="1.0" encoding="utf-8"?>
<!--
    Permite tráfego HTTP em texto puro APENAS para os hosts de desenvolvimento
    local da nossa API privada de fotos (servidor Node rodando na máquina do
    desenvolvedor). Todo o restante continua exigindo HTTPS.
      - 10.0.2.2  = localhost do host visto de dentro do emulador Android
      - localhost / 127.0.0.1 = execução local direta
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
